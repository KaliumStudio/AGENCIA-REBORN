rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Reglas simplificadas para proyecto privado
    // Permite lectura y escritura a cualquier usuario autenticado
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}